### **validation.md**
```markdown
# Validation

## Elastic Discover
- Verified `schtasks.exe` process executions (Event ID 1).
- Confirmed Run Key registry modification events (Event ID 13).

## Evidence
- Screenshots stored in `screenshots/`.
- Text queries stored in `artifacts/`.

## Outcome
- Both persistence techniques were successfully detected in Elastic.
- Dashboards visualized frequency and process activity.